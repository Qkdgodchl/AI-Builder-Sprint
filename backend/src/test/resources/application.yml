spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

upstage:
  api:
    key:
    base-url: https://api.upstage.ai/v1/solar

app:
  bootstrap:
    enabled: false

modusign:
  api:
    email:
    key:
    base-url: https://api.modusign.co.kr
    template-id:
    participant-role: 신청자
    redirect-url: http://127.0.0.1:5173/my-page
  webhook:
    secret: test-webhook-secret
